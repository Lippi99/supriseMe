export default defineNuxtPlugin(async () => {
  const { $googleSignInPlugin } = useNuxtApp();
  
  // The plugin should be automatically registered by nuxt-vue3-google-signin
  // This ensures proper initialization on client-side
});